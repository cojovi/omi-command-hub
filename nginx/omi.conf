limit_req_zone $binary_remote_addr zone=omi_webhooks:10m rate=30r/s;

server {
    listen 80;
    server_name omi.cojovi.com localhost;

    client_max_body_size 50M;

    location /webhooks/ {
        limit_req zone=omi_webhooks burst=50 nodelay;
        proxy_pass http://api:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location / {
        proxy_pass http://api:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# After Certbot, add a second server block for HTTPS (see README.md):
# listen 443 ssl;
# ssl_certificate /etc/letsencrypt/live/omi.cojovi.com/fullchain.pem;
# ssl_certificate_key /etc/letsencrypt/live/omi.cojovi.com/privkey.pem;
